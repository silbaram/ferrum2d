---
name: ferrum-package-qa
description: Use when checking Ferrum2D npm package contents, tarball allowlist, generated Wasm artifacts, package exports, local pack installs, package metadata, or consumer import smoke tests for @ferrum2d/ferrum-web.
---

# Ferrum Package QA

The canonical shared skill body is `.agents/skills/ferrum-package-qa/SKILL.md`.

Before package QA work, read that file and apply it as the source of truth. Do not duplicate or override the canonical workflow here.
