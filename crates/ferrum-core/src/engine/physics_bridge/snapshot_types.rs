#[derive(Clone, Copy, Debug, Default)]
pub(in crate::engine) struct PhysicsQueryResult {
    pub(in crate::engine) entity_id: u32,
    pub(in crate::engine) entity_generation: u32,
    pub(in crate::engine) tile_layer_index: u32,
    pub(in crate::engine) tile_index: u32,
    pub(in crate::engine) point_x: f32,
    pub(in crate::engine) point_y: f32,
    pub(in crate::engine) distance: f32,
}

#[derive(Clone, Copy, Debug, Default)]
pub(in crate::engine) struct PhysicsEntitySnapshot {
    pub(in crate::engine) entity_id: u32,
    pub(in crate::engine) entity_generation: u32,
    pub(in crate::engine) x: f32,
    pub(in crate::engine) y: f32,
    pub(in crate::engine) velocity_x: f32,
    pub(in crate::engine) velocity_y: f32,
    pub(in crate::engine) rotation_radians: f32,
    pub(in crate::engine) angular_velocity_radians_per_second: f32,
    pub(in crate::engine) body_type: u32,
    pub(in crate::engine) body_enabled: bool,
    pub(in crate::engine) is_sleeping: bool,
    pub(in crate::engine) collider_type: u32,
    pub(in crate::engine) collider_enabled: bool,
    pub(in crate::engine) collider_is_trigger: bool,
    pub(in crate::engine) collider_offset_x: f32,
    pub(in crate::engine) collider_offset_y: f32,
    pub(in crate::engine) collider_material_override: bool,
    pub(in crate::engine) collider_restitution: f32,
    pub(in crate::engine) collider_friction: f32,
    pub(in crate::engine) collider_surface_velocity_x: f32,
    pub(in crate::engine) collider_surface_velocity_y: f32,
    pub(in crate::engine) collider_density: f32,
    pub(in crate::engine) collider_contact_baumgarte_bias_scale: f32,
    pub(in crate::engine) collider_max_contact_baumgarte_bias_velocity_scale: f32,
    pub(in crate::engine) collider_contact_position_correction_scale: f32,
    pub(in crate::engine) collider_contact_position_correction_slop_scale: f32,
    pub(in crate::engine) mass: f32,
    pub(in crate::engine) inverse_mass: f32,
    pub(in crate::engine) inertia: f32,
    pub(in crate::engine) inverse_inertia: f32,
    pub(in crate::engine) gravity_scale: f32,
    pub(in crate::engine) linear_damping: f32,
    pub(in crate::engine) angular_damping: f32,
    pub(in crate::engine) restitution: f32,
    pub(in crate::engine) friction: f32,
    pub(in crate::engine) surface_velocity_x: f32,
    pub(in crate::engine) surface_velocity_y: f32,
    pub(in crate::engine) density: f32,
    pub(in crate::engine) contact_baumgarte_bias_scale: f32,
    pub(in crate::engine) max_contact_baumgarte_bias_velocity_scale: f32,
    pub(in crate::engine) contact_position_correction_scale: f32,
    pub(in crate::engine) contact_position_correction_slop_scale: f32,
}

#[derive(Clone, Copy, Debug, Default)]
pub(in crate::engine) struct PhysicsBodyColliderSnapshot {
    pub(in crate::engine) collider_index: u32,
    pub(in crate::engine) collider_type: u32,
    pub(in crate::engine) collider_enabled: bool,
    pub(in crate::engine) collider_is_trigger: bool,
    pub(in crate::engine) collider_offset_x: f32,
    pub(in crate::engine) collider_offset_y: f32,
    pub(in crate::engine) collider_material_override: bool,
    pub(in crate::engine) collider_restitution: f32,
    pub(in crate::engine) collider_friction: f32,
    pub(in crate::engine) collider_surface_velocity_x: f32,
    pub(in crate::engine) collider_surface_velocity_y: f32,
    pub(in crate::engine) collider_density: f32,
    pub(in crate::engine) collider_contact_baumgarte_bias_scale: f32,
    pub(in crate::engine) collider_max_contact_baumgarte_bias_velocity_scale: f32,
    pub(in crate::engine) collider_contact_position_correction_scale: f32,
    pub(in crate::engine) collider_contact_position_correction_slop_scale: f32,
    pub(in crate::engine) collider_category_bits: u32,
    pub(in crate::engine) collider_mask_bits: u32,
}

#[derive(Clone, Copy, Debug, Default)]
pub(in crate::engine) struct PhysicsJointSnapshot {
    pub(in crate::engine) joint_type: u32,
    pub(in crate::engine) joint_index: u32,
    pub(in crate::engine) joint_generation: u32,
    pub(in crate::engine) entity_a_id: u32,
    pub(in crate::engine) entity_a_generation: u32,
    pub(in crate::engine) entity_b_id: u32,
    pub(in crate::engine) entity_b_generation: u32,
    pub(in crate::engine) rest_length: f32,
    pub(in crate::engine) max_length: f32,
    pub(in crate::engine) ratio: f32,
    pub(in crate::engine) slack: bool,
    pub(in crate::engine) reference_angle: f32,
    pub(in crate::engine) break_distance: f32,
    pub(in crate::engine) break_angle: f32,
    pub(in crate::engine) stiffness: f32,
    pub(in crate::engine) damping: f32,
    pub(in crate::engine) angular_stiffness: f32,
    pub(in crate::engine) angular_damping: f32,
    pub(in crate::engine) local_anchor_a_x: f32,
    pub(in crate::engine) local_anchor_a_y: f32,
    pub(in crate::engine) local_anchor_b_x: f32,
    pub(in crate::engine) local_anchor_b_y: f32,
    pub(in crate::engine) local_axis_a_x: f32,
    pub(in crate::engine) local_axis_a_y: f32,
    pub(in crate::engine) ground_anchor_a_x: f32,
    pub(in crate::engine) ground_anchor_a_y: f32,
    pub(in crate::engine) ground_anchor_b_x: f32,
    pub(in crate::engine) ground_anchor_b_y: f32,
    pub(in crate::engine) limit_enabled: bool,
    pub(in crate::engine) continuous_limit: bool,
    pub(in crate::engine) lower_angle: f32,
    pub(in crate::engine) upper_angle: f32,
    pub(in crate::engine) lower_translation: f32,
    pub(in crate::engine) upper_translation: f32,
    pub(in crate::engine) motor_enabled: bool,
    pub(in crate::engine) motor_speed: f32,
    pub(in crate::engine) max_motor_force: f32,
    pub(in crate::engine) max_motor_torque: f32,
    pub(in crate::engine) enabled: bool,
}
