{
  "name": "ferrum2d",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.8.0",
  "scripts": {
    "build": "pnpm -r --filter './packages/*' --filter './examples/*' build",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test",
    "format": "pnpm -r --if-present format"
  }
}
